# 2026-08-11 Tuesday: Inventory and BC Baseline Freeze

## Objectives

- Inventory the local 4070 and 4x3090 server environments.
- Record available storage and existing cached scenario counts.
- Freeze deterministic training and validation manifests.
- Verify that the existing SMART-mini BC checkpoint reloads.
- Measure peak memory and seconds per step with a short smoke run.

## Repository

- Branch: `agent/repro-week-one`
- Machine/host: local Windows laptop and Linux host `3b0261e3c693`
- CATK cache root: `/home/wlk/projects/catk/data/processed`

## Inventory

Run:

```bash
bash scripts/repro_inventory.sh /path/to/cache
```

| Item | Local 4070 | 4x3090 server |
|---|---:|---:|
| GPU count and VRAM | 1x RTX 4070 Laptop, 8 GB | TODO |
| Free disk | 293.5 GB on `C:` | TODO |
| PyTorch | 2.12.1 | TODO |
| CUDA | Runtime 12.6, driver 561.00 | TODO |
| Training cache | Not present | 277 MB |
| Validation cache | Not present | 54 MB |

## Fixed Subsets

| Split | Source | Count | Seed | Manifest |
|---|---|---:|---:|---|
| Training | TODO | 5000 | 817 | `training.manifest.txt` |
| Validation | TODO | 500 | 817 | `validation.manifest.txt` |

```bash
python scripts/make_repro_subset.py /cache/full/training /cache/repro/training \
  --count 5000 --seed 817 --method symlink
python scripts/make_repro_subset.py /cache/full/validation /cache/repro/validation \
  --count 500 --seed 817 --method symlink
```

## Existing BC Baseline

| Field | Value |
|---|---|
| Checkpoint | `/home/wlk/projects/catk/logs/pre_bc-debug/runs/2026-07-13_13-53-09/checkpoints/epoch_000.ckpt` |
| Training scenarios | TODO |
| Epoch | TODO |
| Optimizer steps | TODO |
| Train loss | TODO |
| Validation loss | TODO |
| Top-5 token accuracy | TODO |

## Smoke Run

The launcher accepts additional Hydra overrides after the mode argument:

```bash
CATK_CACHE_ROOT=/cache/repro GPUS=1 BATCH_SIZE=1 \
  TASK_NAME=mini_bc_smoke bash scripts/repro_train.sh bc \
  trainer.max_epochs=1 trainer.limit_train_batches=50 trainer.limit_val_batches=5
```

| Metric | Value |
|---|---:|
| Peak GPU memory | TODO |
| Seconds/step | TODO |
| Final train loss | TODO |
| Checkpoint reload | TODO |
| NaN/OOM | TODO |

The first five-batch launch stopped before dataset construction. The installed
PyG `BaseTransform` requires subclasses to implement `forward`, while upstream
CATK implemented `__call__`. Commit `72d9685` updates both Waymo target
builders to the current PyG transform interface. Rerun the same smoke command
after applying that compatibility patch; this attempt did not exercise the GPU
and therefore produced no throughput, loss, or memory measurement.

## Tuesday Gate

- [ ] Inventory recorded for both available machines.
- [ ] Dataset manifests saved and committed.
- [ ] Existing BC checkpoint provenance recorded.
- [ ] 50-batch smoke run completed.
- [ ] Wednesday batch size and ETA selected from measured throughput.

## Notes and Next Decision

The local `pytorch` environment does not yet contain Hydra and differs from the
repository-pinned PyTorch 2.4.1/Lightning 2.4.0 environment. Windows also adds
avoidable compatibility risk for Waymo, TensorFlow, and PyG extensions. Use the
local machine for repository management and utilities; run the first training
smoke test in the existing Linux environment on the 4x3090 server. The next
decision depends on server free space, cache counts, package versions, and the
existing BC checkpoint provenance.
